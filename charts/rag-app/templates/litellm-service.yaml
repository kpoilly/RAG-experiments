apiVersion: v1
kind: Service
metadata:
  name: {{ include "rag-app.fullname" . }}-litellm-service
spec:
  type: {{ index .Values "litellm" "service" "type" }}
  ports:
    - port: {{ index .Values "litellm" "service" "port" }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/component: litellm
