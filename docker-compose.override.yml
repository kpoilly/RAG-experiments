services:
  cli:
    volumes:
      - ./src/cli:/app

  rag-core:
    volumes:
      - ./src/rag-core:/app
    command: ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8001", "--reload"]

  llm-gateway:
    volumes:
      - ./src/llm-gateway:/app
    command: ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8002", "--reload"]
  
  evaluation-runner:
    volumes:
      - ./src/evaluation-runner:/app
    command: ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8004", "--reload"]
